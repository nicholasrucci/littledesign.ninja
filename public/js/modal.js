function overlay() {
	"use strict";
	var ex = document.getElementById('overlay');
	ex.style.visibility = (ex.style.visibility === 'visible') ? 'hidden' : 'visible';
}
function signup() {
	"use strict";
	var ex = document.getElementById('signup');
	ex.style.visibility = (ex.style.visibility === 'visible') ? 'hidden' : 'visible';
}
function dropdown() {
	"use strict";
	var ex = document.getElementById('dropdown');
	ex.style.visibility = (ex.style.visibility === 'visible') ? 'hidden' : 'visible';
}
function request() {
	"use strict";
	var ex = document.getElementById('request');
	ex.style.visibility = (ex.style.visibility === 'visible') ? 'hidden' : 'visible';
}